# learn-r
A suite of lessons for learning the R programming language

Note: to get callouts to render correctly on Ubuntu (introduced in introductory 
geospatial lesson in September 2024), needed to install texlive-fonts-extra 
(`apt install texlive-fonts-extra`).